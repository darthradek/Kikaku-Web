import type { NextPage } from "next";
import css from "./index.module.scss";

const RegisterPage: NextPage = () => {
  return <div className={css.registerPageWrapper}>register</div>;
};

export default RegisterPage;
