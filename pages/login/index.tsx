import type { NextPage } from "next";
import css from "./index.module.scss";

const LoginPage: NextPage = () => {
  return <div className={css.loginPageWrapper}>login</div>;
};

export default LoginPage;
